node{
    
   
        
        stage ('Initialize') {
            steps {
                def mavenHome = tool name: 'maven 3.6.3' , type: 'maven'
                sh '''
                    echo "PATH = "
                    echo "M2_HOME = ${mavenHome}"
                '''
            }
        }
        
        
        stage('Build') {
            steps {
                sh "${mavenHome}/bin/mvn clean package"
            }
        }
        
            
   
}
